package com.movieapp.swe_project_backend;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication  // Enables component scanning in com.movieapp.swe_project_backend
public class SweProjectBackendApplication {
    public static void main(String[] args) {
        SpringApplication.run(SweProjectBackendApplication.class, args);
    }
}
